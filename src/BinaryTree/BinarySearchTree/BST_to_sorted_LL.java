package BinaryTree.BinarySearchTree;

import BinaryTree.BinaryTreeNode;

public class BST_to_sorted_LL {
    /*public static LinkedListNode<Integer> head;
    public static LinkedListNode< Integer> prev;
    public static LinkedListNode<Integer> constructLinkedList(BinaryTreeNode<Integer> root) {
        head=null;
        prev=null;
        flatten(root);
        return head;

    }

    public static void flatten(BinaryTreeNode<Integer> root){
        if(root==null){
            return;
        }

        flatten(root.left);

        LinkedListNode<Integer> newNode = new LinkedListNode<>(root.data);

        if(prev==null){
            head= newNode;
        }else{
            prev.next = newNode;
        }

        prev = newNode;

        flatten(root.right);

    }*/
}
